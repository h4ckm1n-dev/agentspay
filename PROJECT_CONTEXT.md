# AgentsPay — Project Context Log

A simple append-only log for agent/human collaboration. Append new entries with `>>` prefix and an ISO date.

>> [2026-05-13] Week 1 Step 1 done — scaffolded `services/mcp/` (`agentspay-mcp`) crate using `rmcp 0.16` over stdio. 4 tools advertised (`agentspay_balance`, `agentspay_pay_url`, `agentspay_set_budget`, `agentspay_audit_log`) with in-memory `RwLock<HashMap>` budget store and `Vec<AuditEntry>` audit log. Workspace `Cargo.toml` got 4 additive deps (`rmcp`, `chrono`, `url`, `schemars`) and one new member. `cargo check -p agentspay-mcp`, `cargo build -p agentspay-mcp`, `cargo clippy -p agentspay-mcp -- -D warnings`, `cargo fmt --check` all pass. Manual stdio smoke test via piped `initialize` + `tools/list` JSON-RPC returns all 4 tools with correct JSON schemas.

>> [2026-05-13] Fixed pre-existing `agentspay-proto` codegen bug ("too many leading `super` keywords") by adding `.extern_path(".agentspay.common.v1", "crate::common")` to `packages/proto/build.rs`. `cargo check --workspace` now passes for all 6 crates (proto, mcp, gateway, payment, auth, metering).

>> [2026-05-13] Fixed `serverInfo.name = "rmcp"` UX bug — replaced `Implementation::from_build_env()` (which captures rmcp's own crate env) with an explicit literal reading `env!("CARGO_PKG_NAME")` from the binary's compile context. MCP hosts now correctly see `agentspay-mcp 0.1.0`.

>> [2026-05-13] End-to-end tool exercise: `balance` returns mock state, `set_budget(25,1)` persists + audit-logs, `pay_url("https://example.com/quote","0.50")` validates URL+amount and returns mock `pay_<uuid>`, `audit_log` reads the in-memory Vec. All 4 tools functional. Next: Week 2 — SeaORM ledger models + SQLite migrations + real sandbox `pay_url` against demo Axum provider in `examples/paid-endpoint/`.

>> [2026-05-14] Week 2 done. SeaORM + SQLite ledger live at `~/.agentspay/agentspay-mcp.db` with `wallet/budget/policy/ledger_entry/audit_log` tables + `seaql_migrations`. `services/mcp/src/` now has `db.rs, entities/, migration/, repo.rs, x402.rs`. `agentspay_pay_url` does real reqwest HTTP, parses 402 with x402-Coinbase-CDP format, base64-encodes a sandbox `PaymentPayload`, retries with `X-Payment` header, persists ledger + audit atomically via a SeaORM transaction. New crate `examples/paid-endpoint/` ships the Axum demo provider. End-to-end test verified: `pay_url("http://localhost:3001/quote/AAPL", "0.50")` returns the AAPL quote with `payment_status=paid`, ledger row visible via `sqlite3`, budget persists across binary restarts.

>> [2026-05-14] Budget enforcement added. `pay_url` now refuses when (a) endpoint price > per_call cap, (b) today_spent + price > daily cap. `x402.rs` split into `prepare()` (GET + 402 parse + agent cap) and `complete()` (sign + retry); main.rs runs DB-backed budget check between the two. `repo.rs` gained `today_spending()` (sums ledger entries since UTC midnight). Discovered + fixed concurrency bug: rmcp processes `tools/call` in parallel, so two simultaneous `pay_url` could both pass the budget check against a stale view. Added `tokio::sync::Mutex<()>` on the pay_url critical section. `balance` updated to show `today_spent_usdc, daily_cap_usdc, per_call_cap_usdc, budget_remaining_today_usdc` from real DB state. Known limitation v0.1: `balance` does not acquire the lock so concurrent reads during writes may be stale — fine for human-driven use, revisit for v0.2 stress workloads.

>> [2026-05-14] Week 3 Step 1.1 — **FIRST REAL ON-CHAIN TX**. Funded the agent keypair `GmBDzsdcPBNpeGchxX2GkZTKYtuCKnj7wyHiYaL9zPEm` via Circle USDC faucet + Solana faucet. End-to-end demo signed an SPL `transfer_checked` of 0.10 devnet USDC to `HE5JxfV1VVxrjXNW9ybopevF9uQwyWmJZrYXZxiv7Btv` (the new persistent provider keypair), submitted to `https://api.devnet.solana.com`, confirmed in ~1s. Real signature: `4pGRMVgu7j5itCs7Vf6G9FTQW2Q1B2SjCEKHszLjvF9eVagWvtWq8aJWuYz1JNpBQr4CsbYRXSb9aWAu5hv6jYau` (Solscan: https://solscan.io/tx/4pGRMVgu7j5itCs7Vf6G9FTQW2Q1B2SjCEKHszLjvF9eVagWvtWq8aJWuYz1JNpBQr4CsbYRXSb9aWAu5hv6jYau?cluster=devnet). Ledger row persisted in SQLite. The product is now showable to cherry-picked devs with on-chain proof.

>> [2026-05-14] Fixed ephemeral-provider-keypair gap. `examples/paid-endpoint/src/main.rs::resolve_pay_to()` now load-or-creates a persistent keypair at `~/.agentspay/provider-keypair.json` (mode 0600) as the default. Precedence: `AGENTSPAY_DEMO_PAYTO` env > `AGENTSPAY_PROVIDER_KEYPAIR` env > persistent default > documented `FALLBACK_PAYTO` fallback. This means restarting the demo no longer rotates the recipient — devs can fund the provider once and reuse across runs.

>> [2026-05-14] Wired opt-in facilitator integration. New env vars `AGENTSPAY_USE_FACILITATOR=true` + `AGENTSPAY_FACILITATOR_URL` (default `https://x402.org/facilitator`). When enabled, the seller forwards `paymentPayload + paymentRequirements` to `{facilitator}/verify` then `/settle` instead of submitting to Solana RPC directly. New helper `submit_via_facilitator()` in `examples/paid-endpoint/src/main.rs`. Verified end-to-end against `https://x402.org/facilitator`: the facilitator is alive, accepts our HTTP shape, and responds — but rejects our payload with `invalid_exact_svm_payload_missing_fee_payer`. The wire format we emit is self-sufficient (agent pays its own fees); x402.org expects a fee-sponsored format (transaction with `feePayer` unset so the facilitator can attach itself). This is a known follow-up for Week 3 Step 3: refactor `services/mcp/src/solana.rs::build_usdc_transfer_tx` to emit a `feePayer: None` variant when targeting x402.org facilitators. Direct-RPC path remains the default and works fully.

>> [2026-05-14] Week 3 Step 1 done — real Solana devnet settlement is live. New module `services/mcp/src/wallet.rs` exposes `AgentWallet::load_or_create()` over a Solana-CLI-compatible keypair JSON at `~/.agentspay/keypair.json` (mode 0600, overridable via `AGENTSPAY_KEYPAIR_PATH`). New migration `m20260514_000001_solana.rs` adds a nullable `solana_pubkey TEXT` column on `wallet`; the binary backfills it at startup via the new `LedgerRepo::set_solana_pubkey()` and logs the address on stderr. `x402.rs` was refactored around a `NetworkMode` enum (`Sandbox` / `SolanaDevnet` / `SolanaMainnet`, picked from `AGENTSPAY_NETWORK`, default `solana-devnet`); the real path uses `solana::build_usdc_transfer_tx` + `encode_transaction_b64` to sign a real SPL `transfer_checked` with an idempotent recipient-ATA and ships it as base64 inside the x402 `payload.transaction` field. New error variants: `NetworkMismatch`, `InvalidPayTo`, `Serialize`, `SolanaBuild`, `WalletMissing`. `examples/paid-endpoint/` now exposes two routes: `/quote/:symbol` (sandbox, accepts any non-empty header) and `/real-quote/:symbol` (decodes the bincode Transaction and submits to devnet RPC). Recipient picked from `AGENTSPAY_DEMO_PAYTO` → `AGENTSPAY_PROVIDER_KEYPAIR` → ephemeral fallback. New tool `agentspay_topup_info` returns pubkey + Circle faucet URL + instructions (Circle faucet requires a manual web captcha — no auto-funding). README rewritten around the real-devnet flow; new `scripts/devnet-smoke-test.sh` drives the JSON-RPC sequence and prints the Solscan URL. `cargo check --workspace`, `cargo clippy -p agentspay-mcp -p agentspay-paid-endpoint-demo --all-targets -- -D warnings`, `cargo fmt --check`, and `cargo test -p agentspay-mcp` (9 tests) all pass. End-to-end behaviour verified: sandbox path against `/quote/AAPL` still returns `payment_status=paid` (regression); solana-devnet path against `/real-quote/AAPL` signs a real Tx (logs the payer pubkey on the seller), submits to devnet RPC, and fails at simulation with `Attempt to debit an account but found no record of a prior credit` — the expected failure mode for an unfunded keypair. Used `x402-rs` ecosystem replaced by direct `solana-sdk = "2.3"` + `spl-token` + `spl-associated-token-account` (already wired in v0.2.5); did not add the `x402-rs` crates to keep the dependency surface narrow and avoid macOS-arm64 build friction. Files modified: `services/mcp/src/{wallet.rs (new), main.rs, x402.rs, solana.rs, repo.rs, db.rs (unchanged), entities/wallet.rs, migration/{mod.rs, m20260514_000001_solana.rs (new)}}`, `examples/paid-endpoint/src/main.rs`, `services/mcp/Cargo.toml`, `examples/paid-endpoint/Cargo.toml`, `README.md`, `scripts/devnet-smoke-test.sh (new)`.

>> [2026-05-14] Web-shim Phase 1 done — sandbox surface live. `services/web-shim/src/` now has `config.rs, error.rs, session.rs, state.rs, subprocess.rs, handlers/{mod,health,sandbox}.rs`. Three routes mounted: `GET /api/health`, `POST /api/sandbox/session`, `POST /api/sandbox/call`. Per-session isolated tmpdir (TempDir auto-cleanup, 30min TTL, 60s background sweeper) holds the session's `keypair.json` + `db.sqlite`; subprocess client spawns `agentspay-mcp` with `AGENTSPAY_NETWORK=sandbox` + `AGENTSPAY_KEYPAIR_PATH` + `AGENTSPAY_DATABASE_URL` overrides on every call. Subprocess loop runs the MCP 2025-06-18 stdio handshake (`initialize` + `notifications/initialized` + `tools/call` id=42), bounded by `tokio::time::timeout(10s)`. **Critical fix discovered during smoke test**: rmcp treats stdin EOF as shutdown — so we now hold stdin open until the tools/call response arrives, then kill+reap. Tool whitelist enforces the five public tools (balance/pay_url/set_budget/audit_log/topup_info). `ShimError` maps to HTTP with request-id JSON envelopes + `retry-after` header on `RateLimited`. Verified end-to-end: `/api/sandbox/session` returns `{session_id, expires_in_secs:1800}`, `/api/sandbox/call` with tool=`agentspay_balance` returns `available_usdc=100.00, budget_remaining_today_usdc=50.00` in ~15ms. `cargo check --workspace` clean, `cargo test -p agentspay-web-shim` 2 passed (session create+expire), `cargo clippy -p agentspay-web-shim --all-targets -- -D warnings` clean (Phase 2-4 unused-yet fields annotated `#[allow(dead_code)]`). 3 new commits on `feat/website-and-live-demo` (branch is at 7 total commits).

>> 2026-05-15 [rust-dev] Phase 4 web-shim DONE:
   - 4.1: SessionStore::new_with_redis (opt-in via AGENTSPAY_REDIS_URL), mirrors session metadata into Redis with TTL.
   - 4.2: ratelimit.rs sliding-window limiter, in-memory + Redis backends, plumbed through AppState.
   - 4.3: /api/sandbox/session (30/min per IP) and /api/sandbox/call (60/min per session_id) now return 429 when over cap.
   - Files: services/web-shim/src/{session.rs, ratelimit.rs (new), state.rs, handlers/sandbox.rs, main.rs}.
   - Verified: cargo test -p agentspay-web-shim → 3 passed; cargo check --workspace clean; smoke test 30×200 + 1×429.
   - Branch feat/website-and-live-demo: 15 commits ahead of main. Next steps: Phase 5 (frontend wire-up for 429 retry-after).

>> 2026-05-15 [rust-dev] Phase 5 web-shim DONE — devnet integration live:
   - 5.1: devnet_wallet.rs reads ~/.agentspay/keypair.json (32-byte secret || 32-byte pubkey), derives base58 pubkey, polls Solana devnet RPC for SOL (getBalance) + USDC (getTokenAccountsByOwner on USDC devnet mint 4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU). Healthy when SOL >= 0.05 AND USDC >= 2.0. Two new handlers in handlers/devnet.rs: GET /api/devnet/wallet-status (60/min/IP rate) and POST /api/devnet/trigger (1/hour/IP rate). Trigger picks a random symbol from [AAPL,MSFT,GOOG,NVDA,AMZN] and calls agentspay_pay_url via subprocess on solana-devnet with max_amount_usdc=0.50.
   - 5.2: latest_tx.rs Arc<RwLock<Option<LatestTx>>> cache, populated by trigger, exposed via GET /api/stats/latest-tx (returns signature/amount/explorer_url/age_seconds, hides entries older than 24h). AppState extended with SharedLatestTx.
   - 5.3: main.rs wires the three new routes + constructs LatestTxCache.
   - Files: services/web-shim/Cargo.toml (+bs58), services/web-shim/src/{devnet_wallet.rs (new), latest_tx.rs (new), state.rs, main.rs, handlers/{mod.rs, devnet.rs (new), stats.rs (new)}}.
   - Verified: cargo check --workspace clean; cargo test -p agentspay-web-shim → 3 passed; cargo clippy -p agentspay-web-shim -- -D warnings clean.
   - Real on-chain smoke test against funded agent keypair GmBDzsdcPBNpeGchxX2GkZTKYtuCKnj7wyHiYaL9zPEm: wallet-status returned 9.998 SOL + 19.9 USDC healthy; trigger paid 0.10 USDC for GOOG quote, real signature 3EUyjsdN7Y2ZHTUFMaNn3Y3TyMsGcK673Bis5oMw49RgTPGXhUJJiRYyG2JYrkkQypfszJH9FuRBPScTmXh2BFJU (Solscan: https://solscan.io/tx/3EUyjsdN7Y2ZHTUFMaNn3Y3TyMsGcK673Bis5oMw49RgTPGXhUJJiRYyG2JYrkkQypfszJH9FuRBPScTmXh2BFJU?cluster=devnet), latency 2.3s; latest-tx echoed same signature with age 6s; second trigger correctly returned 429 retry-after=3590.
   - Branch feat/website-and-live-demo: 18 commits ahead of main. Next steps: Phase 6 frontend integration of the hero counter + one-click trigger button.

>> 2026-05-15 [deploy] Phase 8 Docker stack DONE (5 commits, 29 total ahead of main):
   - 8.1: docker/Dockerfile.shim — multi-stage rust:1.83-slim builder, produces both agentspay-web-shim + agentspay-mcp binaries, copies both to debian:bookworm-slim runtime.
   - 8.2: docker/Dockerfile.paid-endpoint — same pattern, single binary agentspay-paid-endpoint-demo.
   - 8.3: docker/Dockerfile.web — node:22-slim 3-stage (deps/builder/runtime), Next.js standalone output. apps/frontend/public/.gitkeep added to satisfy COPY.
   - 8.4: docker/Caddyfile (ACME TLS, /api/* → shim:8080, /* → web:3000), docker/docker-compose.yml (5 services: caddy/web/shim/paid-endpoint/redis + 4 named volumes), docker/docker-compose.local.yml (caddy disabled via profiles, ports exposed), docker/.env.example.
   - 8.5: docker/scripts/refill-wallet.sh + backup-wallet.sh (both chmod +x), docker/README.md runbook.
   - Smoke test: Docker daemon confirmed running. Full build initiated (rust:1.83-slim 317MB pull in progress at commit time — slow network, first pull only). All artifacts verified on disk. Root docker-compose.yml (legacy v2 stack) untouched.

>> 2026-05-15 — web-shim: LatestTxCache now persists to SQLite (latest_tx_cache STRICT table in the same devnet ledger DB used by agentspay-mcp). load(path) on boot hydrates the singleton row; set() UPSERTs best-effort (warn-on-fail). New unit test persists_across_new_cache_instance. Files: services/web-shim/{Cargo.toml,src/latest_tx.rs,src/main.rs}.

>> 2026-05-15 [rust-dev] agentspay-mcp DX polish — starship-style stderr formatter live. New module services/mcp/src/pretty.rs renders a 7-line boot banner (brand+version, network+toggle hint, truncated pubkey + keypair path + perms, ledger path + audit count, ready+tool count) and one info-dense line per tool call (HH:MM:SS  tool  network  latency  ✓/✗ detail). Detection is zero-dep: NO_COLOR + IsTerminal for ANSI, LANG/LC_ALL/LC_CTYPE UTF-8 for Nerd-font glyphs; falls back to ASCII tokens ([net]/[key]/[db ]/[ok ], t=, +, x, -) when piped to a file or non-UTF8 locale. Removed the 4 boilerplate startup tracing-info lines (opening db / agent wallet ready / x402 network mode / starting on stdio) that the banner replaces; all other tracing events (warn/error) stay. Each handler (balance / pay_url / set_budget / audit_log / topup_info) now measures latency with Instant::now() and emits a pretty event line; pay_url emits on all four error branches (invalid input / per-call-cap / daily-cap / settle-error) plus the no-payment-required + paid success paths. All writes go to std::io::stderr().lock() — stdout remains reserved for MCP JSON-RPC framing. cargo check --workspace clean, cargo clippy -p agentspay-mcp --all-targets -- -D warnings clean, cargo fmt --check clean, cargo test -p agentspay-mcp → 14 passed (9 existing + 5 new in pretty::tests). Smoke-tested both paths: non-TTY (stderr → file) emits 0 ANSI escapes, PTY mode emits the full colorized banner with Nerd-font glyphs. Files: services/mcp/src/{pretty.rs (new), main.rs}.

>> 2026-05-16 [rust-dev] agentspay-mcp pretty.rs — domain-aware detail tokenization. New `tokenize_detail(&str) -> Vec<DetailSegment<'_>>` + `render_detail(&str, fn(&str) -> String) -> String` recognize 6 token classes (signature 88-char base58, pubkey 32-44 base58, USDC decimals, RFC3339 timestamps, UUIDs, URLs) plus a truncated-sig form (`4pGR…jYau`) and `key=value` segments where USDC-implying keys (daily/per_call/cap/amount/balance/price/spent/value/cost) color the value mint green and the key + `=` dim grey. Palette matches the frontend syntax-highlighter that landed in 9db7d1f (signature #c084fc/141, pubkey #7dd3fc/117, usdc #34d399/79, timestamp #fcd34d/222, uuid #94a3b8/145, url #10b981/38+underline, kv-key 245). Wired into `emit_tool_event` — `ToolOutcome::Ok` now flows through `render_detail(detail, ok_color)` and `ToolOutcome::Err` through `render_detail(detail, fail_color)`; plain spans inherit the outcome's base color (green/red). Hand-rolled tokenizer (~200 lines, no new deps — `regex` is not in workspace), greedy left-to-right, word-boundary-aware (won't gobble mid-word base58 runs). No public API change to `emit_tool_event` / `print_banner` — `main.rs` callers untouched. All 7 existing color helpers preserved; 7 new helpers added (`color_signature/pubkey/usdc/timestamp/uuid/url/kv_key`). cargo check --workspace clean, cargo clippy -p agentspay-mcp --all-targets -- -D warnings clean, cargo fmt --check clean, cargo test -p agentspay-mcp → 24 passed (was 14 — added 10 new pretty::tests::tokenize_detail_* and render_detail_* covering signature+usdc/truncated-sig/kv-usdc/url-underline/uuid/unicode-soup/empty/plain-passthrough). PTY smoke verified: set_budget detail `daily=25.00 per_call=1.00` emits 2× ANSI `[38;5;79m` (mint-green USDC values) and ≥4× ANSI `[38;5;245m` (dim-grey keys + `=` separators); non-TTY path collapses to plain ASCII automatically via the existing `supports_ansi()` short-circuit in `fg256`. Files: services/mcp/src/pretty.rs (single-file change). 1 new commit on feat/website-and-live-demo (now 39 commits ahead of main).
