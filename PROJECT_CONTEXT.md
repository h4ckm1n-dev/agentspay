# AgentsPay — Project Context Log

A simple append-only log for agent/human collaboration. Append new entries with `>>` prefix and an ISO date.

>> [2026-05-13] Week 1 Step 1 done — scaffolded `services/mcp/` (`agentspay-mcp`) crate using `rmcp 0.16` over stdio. 4 tools advertised (`agentspay_balance`, `agentspay_pay_url`, `agentspay_set_budget`, `agentspay_audit_log`) with in-memory `RwLock<HashMap>` budget store and `Vec<AuditEntry>` audit log. Workspace `Cargo.toml` got 4 additive deps (`rmcp`, `chrono`, `url`, `schemars`) and one new member. `cargo check -p agentspay-mcp`, `cargo build -p agentspay-mcp`, `cargo clippy -p agentspay-mcp -- -D warnings`, `cargo fmt --check` all pass. Manual stdio smoke test via piped `initialize` + `tools/list` JSON-RPC returns all 4 tools with correct JSON schemas.

>> [2026-05-13] Fixed pre-existing `agentspay-proto` codegen bug ("too many leading `super` keywords") by adding `.extern_path(".agentspay.common.v1", "crate::common")` to `packages/proto/build.rs`. `cargo check --workspace` now passes for all 6 crates (proto, mcp, gateway, payment, auth, metering).

>> [2026-05-13] Fixed `serverInfo.name = "rmcp"` UX bug — replaced `Implementation::from_build_env()` (which captures rmcp's own crate env) with an explicit literal reading `env!("CARGO_PKG_NAME")` from the binary's compile context. MCP hosts now correctly see `agentspay-mcp 0.1.0`.

>> [2026-05-13] End-to-end tool exercise: `balance` returns mock state, `set_budget(25,1)` persists + audit-logs, `pay_url("https://example.com/quote","0.50")` validates URL+amount and returns mock `pay_<uuid>`, `audit_log` reads the in-memory Vec. All 4 tools functional. Next: Week 2 — SeaORM ledger models + SQLite migrations + real sandbox `pay_url` against demo Axum provider in `examples/paid-endpoint/`.

>> [2026-05-14] Week 2 done. SeaORM + SQLite ledger live at `~/.agentspay/agentspay-mcp.db` with `wallet/budget/policy/ledger_entry/audit_log` tables + `seaql_migrations`. `services/mcp/src/` now has `db.rs, entities/, migration/, repo.rs, x402.rs`. `agentspay_pay_url` does real reqwest HTTP, parses 402 with x402-Coinbase-CDP format, base64-encodes a sandbox `PaymentPayload`, retries with `X-Payment` header, persists ledger + audit atomically via a SeaORM transaction. New crate `examples/paid-endpoint/` ships the Axum demo provider. End-to-end test verified: `pay_url("http://localhost:3001/quote/AAPL", "0.50")` returns the AAPL quote with `payment_status=paid`, ledger row visible via `sqlite3`, budget persists across binary restarts.

>> [2026-05-14] Budget enforcement added. `pay_url` now refuses when (a) endpoint price > per_call cap, (b) today_spent + price > daily cap. `x402.rs` split into `prepare()` (GET + 402 parse + agent cap) and `complete()` (sign + retry); main.rs runs DB-backed budget check between the two. `repo.rs` gained `today_spending()` (sums ledger entries since UTC midnight). Discovered + fixed concurrency bug: rmcp processes `tools/call` in parallel, so two simultaneous `pay_url` could both pass the budget check against a stale view. Added `tokio::sync::Mutex<()>` on the pay_url critical section. `balance` updated to show `today_spent_usdc, daily_cap_usdc, per_call_cap_usdc, budget_remaining_today_usdc` from real DB state. Known limitation v0.1: `balance` does not acquire the lock so concurrent reads during writes may be stale — fine for human-driven use, revisit for v0.2 stress workloads.

>> [2026-05-14] Week 3 Step 1.1 — **FIRST REAL ON-CHAIN TX**. Funded the agent keypair `GmBDzsdcPBNpeGchxX2GkZTKYtuCKnj7wyHiYaL9zPEm` via Circle USDC faucet + Solana faucet. End-to-end demo signed an SPL `transfer_checked` of 0.10 devnet USDC to `HE5JxfV1VVxrjXNW9ybopevF9uQwyWmJZrYXZxiv7Btv` (the new persistent provider keypair), submitted to `https://api.devnet.solana.com`, confirmed in ~1s. Real signature: `4pGRMVgu7j5itCs7Vf6G9FTQW2Q1B2SjCEKHszLjvF9eVagWvtWq8aJWuYz1JNpBQr4CsbYRXSb9aWAu5hv6jYau` (Solscan: https://solscan.io/tx/4pGRMVgu7j5itCs7Vf6G9FTQW2Q1B2SjCEKHszLjvF9eVagWvtWq8aJWuYz1JNpBQr4CsbYRXSb9aWAu5hv6jYau?cluster=devnet). Ledger row persisted in SQLite. The product is now showable to cherry-picked devs with on-chain proof.

>> [2026-05-14] Fixed ephemeral-provider-keypair gap. `examples/paid-endpoint/src/main.rs::resolve_pay_to()` now load-or-creates a persistent keypair at `~/.agentspay/provider-keypair.json` (mode 0600) as the default. Precedence: `AGENTSPAY_DEMO_PAYTO` env > `AGENTSPAY_PROVIDER_KEYPAIR` env > persistent default > documented `FALLBACK_PAYTO` fallback. This means restarting the demo no longer rotates the recipient — devs can fund the provider once and reuse across runs.

>> [2026-05-14] Wired opt-in facilitator integration. New env vars `AGENTSPAY_USE_FACILITATOR=true` + `AGENTSPAY_FACILITATOR_URL` (default `https://x402.org/facilitator`). When enabled, the seller forwards `paymentPayload + paymentRequirements` to `{facilitator}/verify` then `/settle` instead of submitting to Solana RPC directly. New helper `submit_via_facilitator()` in `examples/paid-endpoint/src/main.rs`. Verified end-to-end against `https://x402.org/facilitator`: the facilitator is alive, accepts our HTTP shape, and responds — but rejects our payload with `invalid_exact_svm_payload_missing_fee_payer`. The wire format we emit is self-sufficient (agent pays its own fees); x402.org expects a fee-sponsored format (transaction with `feePayer` unset so the facilitator can attach itself). This is a known follow-up for Week 3 Step 3: refactor `services/mcp/src/solana.rs::build_usdc_transfer_tx` to emit a `feePayer: None` variant when targeting x402.org facilitators. Direct-RPC path remains the default and works fully.

>> [2026-05-14] Week 3 Step 1 done — real Solana devnet settlement is live. New module `services/mcp/src/wallet.rs` exposes `AgentWallet::load_or_create()` over a Solana-CLI-compatible keypair JSON at `~/.agentspay/keypair.json` (mode 0600, overridable via `AGENTSPAY_KEYPAIR_PATH`). New migration `m20260514_000001_solana.rs` adds a nullable `solana_pubkey TEXT` column on `wallet`; the binary backfills it at startup via the new `LedgerRepo::set_solana_pubkey()` and logs the address on stderr. `x402.rs` was refactored around a `NetworkMode` enum (`Sandbox` / `SolanaDevnet` / `SolanaMainnet`, picked from `AGENTSPAY_NETWORK`, default `solana-devnet`); the real path uses `solana::build_usdc_transfer_tx` + `encode_transaction_b64` to sign a real SPL `transfer_checked` with an idempotent recipient-ATA and ships it as base64 inside the x402 `payload.transaction` field. New error variants: `NetworkMismatch`, `InvalidPayTo`, `Serialize`, `SolanaBuild`, `WalletMissing`. `examples/paid-endpoint/` now exposes two routes: `/quote/:symbol` (sandbox, accepts any non-empty header) and `/real-quote/:symbol` (decodes the bincode Transaction and submits to devnet RPC). Recipient picked from `AGENTSPAY_DEMO_PAYTO` → `AGENTSPAY_PROVIDER_KEYPAIR` → ephemeral fallback. New tool `agentspay_topup_info` returns pubkey + Circle faucet URL + instructions (Circle faucet requires a manual web captcha — no auto-funding). README rewritten around the real-devnet flow; new `scripts/devnet-smoke-test.sh` drives the JSON-RPC sequence and prints the Solscan URL. `cargo check --workspace`, `cargo clippy -p agentspay-mcp -p agentspay-paid-endpoint-demo --all-targets -- -D warnings`, `cargo fmt --check`, and `cargo test -p agentspay-mcp` (9 tests) all pass. End-to-end behaviour verified: sandbox path against `/quote/AAPL` still returns `payment_status=paid` (regression); solana-devnet path against `/real-quote/AAPL` signs a real Tx (logs the payer pubkey on the seller), submits to devnet RPC, and fails at simulation with `Attempt to debit an account but found no record of a prior credit` — the expected failure mode for an unfunded keypair. Used `x402-rs` ecosystem replaced by direct `solana-sdk = "2.3"` + `spl-token` + `spl-associated-token-account` (already wired in v0.2.5); did not add the `x402-rs` crates to keep the dependency surface narrow and avoid macOS-arm64 build friction. Files modified: `services/mcp/src/{wallet.rs (new), main.rs, x402.rs, solana.rs, repo.rs, db.rs (unchanged), entities/wallet.rs, migration/{mod.rs, m20260514_000001_solana.rs (new)}}`, `examples/paid-endpoint/src/main.rs`, `services/mcp/Cargo.toml`, `examples/paid-endpoint/Cargo.toml`, `README.md`, `scripts/devnet-smoke-test.sh (new)`.
